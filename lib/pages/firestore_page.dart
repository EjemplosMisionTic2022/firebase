import 'package:flutter/material.dart';

class FireStorePage extends StatelessWidget {
  const FireStorePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('FireStorePage')),
    );
  }
}
